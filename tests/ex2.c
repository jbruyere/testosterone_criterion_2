/*
** EPITECH PROJECT, 2019
** TU
** File description:
** ex1
*/

#include <criterion/criterion.h>
#include "my.h"

/*

Exercice 2:
Implementer la fonction du fichier sources/ex2.c: int my_func(int a, int b, int c)
Afin que le test suivant soit valide

Il est interdit de comparer des valeurs exacts
(Example:   a == 1 && b == 2 && c == 3 -> Interdit
            a + 2 == 12 || c - 1 == 2 -> Interdit)

Mais il est autorisé de faire ce genre de comparaison:
a + b <= c + 4
2 * c == b

Decommentez le Test pour essayer

Quel est l'interêt de faire coder le test avant de coder la fonction ?

*/

Test(test_ex2, ex2)
{
    // cr_expect(my_func(-2, 2, 3) == 5);
    // cr_expect(my_func(2, 3, 4) == -1);
    // cr_expect(my_func(16, 12, 5) == -12);
    // cr_expect(my_func(3, 4, 7) == 3);
}